export const metadata = {
  title: {
    template: "%s | Questinnaires",
    default: "Sample Questionnaire",
  },
};

export default function SurveyLayout({ children }) {
  return <div>{children}</div>;
}
