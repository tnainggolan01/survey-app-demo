export const metadata = {
  title: {
    template: "%s | Questionnaires",
    default: "Questionnaire List",
  },
};

export default function SurveyLayout({ children }) {
  return <div>{children}</div>;
}
